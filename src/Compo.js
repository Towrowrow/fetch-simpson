import React from 'react';

const Generatequote = ({ simpson }) => {
  return (
    <div className="quote">
      <button onClick={simpson}>Get quote</button>
    </div>
  );
};

export default Generatequote;
